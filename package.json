{
  "name": "lecture_fp",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev",
    "export": "slidev export",
    "rm": "rm -rf docs",
    "mv": "mv dist docs",
    "git": "git add . && git commit -m deployed && git push origin master",
    "deploy": "npm run rm && npm run build && npm run mv && npm run git"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.9",
    "@slidev/theme-default": "^0.25.0",
    "@slidev/theme-seriph": "^0.25.0",
    "slidev-theme-penguin": "^2.2.0",
    "vue": "^3.4.27"
  }
}
